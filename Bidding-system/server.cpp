#include<iomanip>
#include<iostream>
#include<string>
using namespace std; 
struct client{
    string name;
    double bid;
    string item;
}
client clients[4];
void bidBox(string bidder,double price, string it)
{
    client tmp;
    tmp.name=bidder;
    tmp.bid=price;
    tmp.item=it;

    for()
        if(tmp.item==items[i].name)
            if(tmp.bid>items.price)
            {
                items[i].price=bid;
            }
}